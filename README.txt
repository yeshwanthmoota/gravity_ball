This in one of probable implementations of how the gravity in the code works
This is my self implementation, The traditional implementation could be different
This uses time module's time.time() function => it uses the time measurement since the epoch

epoch is the The time() function returns the number of seconds passed since epoch. For Unix system,
January 1, 1970, 00:00:00 at UTC(Universal co-ordinated) is epoch (the point where time begins).
